@model IEnumerable<codeTongCucThienTai.Models.NhiemVu>
@if (Model != null && Model.Count() > 0)
{
    <ul>
        @foreach (var item in Model)
        {
            <li>@Html.Raw(item.nhiemVuName)</li>
        }
    </ul>
}
